# Task: add greet helper

Add `src/greet.py` exposing:

```python
def greet(name: str) -> str
```

Requirements:
1. Returns exactly `Hello, <name>!` for a non-empty name.
2. Raises `ValueError` when `name` is empty or whitespace-only.
